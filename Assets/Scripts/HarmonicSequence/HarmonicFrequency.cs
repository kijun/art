public struct HarmonicFrequency {
    public float frequency;
    public Complex coefficient;

    public HarmonicFrequency(float frequency, Complex coefficient) {
        this.frequency = frequency;
        this.coefficient = coefficient;
    }
}
